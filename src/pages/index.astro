---
import Layout from '../layouts/Layout.astro';

const title = 'Daniele Odorico — Full-stack Developer';
const subtitle = 'Costruisco interfacce pulite e affidabili, con attenzione a performance e DX.';
---

<Layout title={title}>

  <!-- HERO -->
  <section class="mb-16 sm:mb-24">
    <h1 class="text-4xl sm:text-6xl font-bold leading-tight mb-4 text-[#f2f2f2]">
      Ciao, sono <span class="text-[#00bcd4]">Daniele Odorico</span>
    </h1>
    <p class="text-xl sm:text-2xl text-[#cfcfcf] mb-6">
      Full-stack developer — <span class="text-[#00bcd4]">Angular</span>, TypeScript, Node.
    </p>

    <p class="text-lg text-[#b8b8b8] max-w-2xl mb-8">
      Mi occupo di front-end strutturato, integrazione API e cura dei dettagli UI/UX.
      Qui condivido <span class="tag">#progetti</span>, <span class="tag">#competenze</span> e qualche riflessione sul mio percorso.
    </p>

    <div class="flex flex-wrap gap-3">
      <a href="/projects" class="btn btn-lg sm:min-w-48">Guarda i Progetti</a>
      <a href="/blog" class="btn btn-outline sm:min-w-48">Leggi il Blog</a>
      <a href="/contact" class="btn-link sm:min-w-32">Contatti →</a>
    </div>

    <!-- status riga "di sistema" -->
    <p class="mt-6 text-sm text-[#7b7b7b] font-mono">
      <span class="text-[#00bcd4]">&gt; system.log</span>("site status: online")
    </p>
  </section>

  <!-- HIGHLIGHTS / CARDS INTRO -->
  <section class="grid gap-6 sm:gap-8 sm:grid-cols-3">
    <article class="card">
      <h3 class="card-title">Competenze</h3>
      <p class="card-body">
        Angular, TypeScript, RxJS, HTML/SCSS, Node/Express, REST, JWT, testing.
      </p>
      <a href="/skills" class="card-link">Scopri di più</a>
    </article>

    <article class="card">
      <h3 class="card-title">Progetti</h3>
      <p class="card-body">
        Web app e side-projects con focus su performance, accessibilità e DX.
      </p>
      <a href="/projects" class="card-link">Vai al portfolio</a>
    </article>

    <article class="card">
      <h3 class="card-title">Riflessioni</h3>
      <p class="card-body">
        Note brevi su sviluppo, organizzazione del lavoro e ciò che trovo interessante.
      </p>
      <a href="/blog" class="card-link">Apri il blog</a>
    </article>
  </section>

  <style>
    @import url('https://fonts.googleapis.com/css2?family=VT323&family=JetBrains+Mono:wght@400;600&display=swap');

    .font-mono { font-family: 'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, monospace; }

    /* Tag stile “code” */
    .tag{
      font-family: 'JetBrains Mono', ui-monospace, monospace;
      color:#00bcd4;
      background: rgba(255,255,255,.06);
      padding:.15rem .4rem;
      border-radius:.35rem;
    }

    /* Bottoni base (coerenti con palette) */
    .btn{
      display:inline-block;
      padding:.8rem 1rem;
      border-radius:.6rem;
      background:#00bcd4;
      color:#0b0b0b;
      font-weight:600;
      transition:transform .15s ease, box-shadow .2s ease, background .2s ease;
    }
    .btn:hover{ transform:translateY(-1px); box-shadow:0 6px 16px rgba(0,188,212,.25); }
    .btn.btn-lg{ padding:1rem 1.25rem; }

    .btn-outline{
      background:transparent;
      color:#d16ba5;
      border:1px solid rgba(209,107,165,.55);
    }
    .btn-outline:hover{ background:rgba(209,107,165,.12); }

    .btn-link{
      color:#00bcd4;
      padding:.8rem .2rem;
      border-bottom:1px dashed rgba(0,188,212,.5);
    }
    .btn-link:hover{ color:#d16ba5; border-bottom-color:#d16ba5; }

    /* Cards intro */
    .card{
      background: rgba(0,0,0,.45);
      border:1px solid rgba(255,255,255,.08);
      border-radius:14px;
      padding:1.25rem;
      transition: border-color .2s ease, box-shadow .2s ease, transform .15s ease;
    }
    .card:hover{
      border-color: rgba(0,188,212,.45);
      box-shadow: 0 0 18px rgba(0,188,212,.18);
      transform: translateY(-2px);
    }
    .card-title{
      color:#f2f2f2;
      font-weight:700;
      margin-bottom:.35rem;
    }
    .card-body{ color:#bfbfbf; margin-bottom:.6rem; }
    .card-link{
      color:#00bcd4; border-bottom:1px dashed rgba(0,188,212,.5);
    }
    .card-link:hover{ color:#d16ba5; border-bottom-color:#d16ba5; }
  </style>

</Layout>
